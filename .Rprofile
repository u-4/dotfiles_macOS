
# Rcmdrを立ち上げる時はEZRプラグインを読み込む
options(Rcmdr=list(plugins='RcmdrPlugin.EZR'))

#
# R.appを立ち上げる時にRcmdrを立ち上げる
#
# library(Rcmdr)
# local({
# old <- getOption('defaultPackages')
# options(defaultPackages = c(old, 'Rcmdr'))
# })

